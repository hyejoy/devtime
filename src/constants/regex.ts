export const emailRegex = /^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}$/;
/**
 * [최신 비밀번호 정규식]
 * 1. 최소 8자 이상
 * 2. 영문(대/소문자) 필수 포함
 * 3. 숫자 필수 포함
 * 4. 특수문자 선택적 허용 (입력 가능하도록 범위를 넓힘)
 */
export const passwordRegex =
  /^(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d!@#$%^&*()_+={}\[\]:;"'<>,.?/\\|~`-]{8,}$/;
